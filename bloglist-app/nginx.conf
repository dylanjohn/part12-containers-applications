events { }

http {
  server {
    listen 80;
    
    # API requests go to backend
    location /api/ {
      proxy_pass http://backend:3001/api/;
    }
    
    # Everything else goes to frontend (now nginx serving static files)
    location / {
      proxy_pass http://frontend:80;
    }
  }
}