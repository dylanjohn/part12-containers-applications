events { }

http {
  server {
    listen 80;
    
    # API requests go to backend
    location /api/ {
      proxy_pass http://server:3000/;
    }
    
    # Everything else goes to frontend 
    location / {
      proxy_pass http://app:80;  # Frontend container now runs nginx on port 80
    }
  }
}